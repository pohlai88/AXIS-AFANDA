/**
 * Consultations & Meetings — Type Definitions
 */

/**
 * NOTE: This file exists for legacy component imports.
 * The single source of truth for domain types is `app/lib/types`.
 */

export type { Meeting, MeetingStats, HeatmapDay } from '@/app/lib/types';
